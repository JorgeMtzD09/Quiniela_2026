// Calendario oficial fase de grupos — orden FIFA #1-#72, horarios en ET (EDT, UTC-4 en junio 2026)
// Nombres en español (igual que la BD). Fuente: calendario FIFA / worldcuppass.com (actualizado jun 2026)

export const FIXTURES = [
  { id: 1, local: 'México', visitante: 'Sudáfrica', date: '2026-06-11', timeET: '15:00' },
  { id: 2, local: 'Corea del Sur', visitante: 'Chequia', date: '2026-06-11', timeET: '22:00' },
  { id: 3, local: 'Canadá', visitante: 'Bosnia y Herzegovina', date: '2026-06-12', timeET: '15:00' },
  { id: 4, local: 'Estados Unidos', visitante: 'Paraguay', date: '2026-06-12', timeET: '21:00' },
  { id: 5, local: 'Catar', visitante: 'Suiza', date: '2026-06-13', timeET: '15:00' },
  { id: 6, local: 'Brasil', visitante: 'Marruecos', date: '2026-06-13', timeET: '18:00' },
  { id: 7, local: 'Haití', visitante: 'Escocia', date: '2026-06-13', timeET: '21:00' },
  { id: 8, local: 'Australia', visitante: 'Turquía', date: '2026-06-13', timeET: '00:00' },
  { id: 9, local: 'Alemania', visitante: 'Curazao', date: '2026-06-14', timeET: '13:00' },
  { id: 10, local: 'Países Bajos', visitante: 'Japón', date: '2026-06-14', timeET: '16:00' },
  { id: 11, local: 'Costa de Marfil', visitante: 'Ecuador', date: '2026-06-14', timeET: '19:00' },
  { id: 12, local: 'Suecia', visitante: 'Túnez', date: '2026-06-14', timeET: '22:00' },
  { id: 13, local: 'España', visitante: 'Cabo Verde', date: '2026-06-15', timeET: '12:00' },
  { id: 14, local: 'Bélgica', visitante: 'Egipto', date: '2026-06-15', timeET: '15:00' },
  { id: 15, local: 'Arabia Saudita', visitante: 'Uruguay', date: '2026-06-15', timeET: '18:00' },
  { id: 16, local: 'Irán', visitante: 'Nueva Zelanda', date: '2026-06-15', timeET: '21:00' },
  { id: 17, local: 'Francia', visitante: 'Senegal', date: '2026-06-16', timeET: '15:00' },
  { id: 18, local: 'Irak', visitante: 'Noruega', date: '2026-06-16', timeET: '18:00' },
  { id: 19, local: 'Argentina', visitante: 'Argelia', date: '2026-06-16', timeET: '21:00' },
  { id: 20, local: 'Austria', visitante: 'Jordania', date: '2026-06-17', timeET: '00:00' },
  { id: 21, local: 'Portugal', visitante: 'RD Congo', date: '2026-06-17', timeET: '13:00' },
  { id: 22, local: 'Inglaterra', visitante: 'Croacia', date: '2026-06-17', timeET: '16:00' },
  { id: 23, local: 'Ghana', visitante: 'Panamá', date: '2026-06-17', timeET: '19:00' },
  { id: 24, local: 'Uzbekistán', visitante: 'Colombia', date: '2026-06-17', timeET: '22:00' },
  { id: 25, local: 'Chequia', visitante: 'Sudáfrica', date: '2026-06-18', timeET: '12:00' },
  { id: 26, local: 'Suiza', visitante: 'Bosnia y Herzegovina', date: '2026-06-18', timeET: '15:00' },
  { id: 27, local: 'Canadá', visitante: 'Catar', date: '2026-06-18', timeET: '18:00' },
  { id: 28, local: 'México', visitante: 'Corea del Sur', date: '2026-06-18', timeET: '21:00' },
  { id: 29, local: 'Estados Unidos', visitante: 'Australia', date: '2026-06-19', timeET: '15:00' },
  { id: 30, local: 'Escocia', visitante: 'Marruecos', date: '2026-06-19', timeET: '18:00' },
  { id: 31, local: 'Brasil', visitante: 'Haití', date: '2026-06-19', timeET: '21:00' },
  { id: 32, local: 'Turquía', visitante: 'Paraguay', date: '2026-06-20', timeET: '00:00' },
  { id: 33, local: 'Países Bajos', visitante: 'Suecia', date: '2026-06-20', timeET: '13:00' },
  { id: 34, local: 'Alemania', visitante: 'Costa de Marfil', date: '2026-06-20', timeET: '16:00' },
  { id: 35, local: 'Ecuador', visitante: 'Curazao', date: '2026-06-20', timeET: '20:00' },
  { id: 36, local: 'Túnez', visitante: 'Japón', date: '2026-06-21', timeET: '00:00' },
  { id: 37, local: 'España', visitante: 'Arabia Saudita', date: '2026-06-21', timeET: '12:00' },
  { id: 38, local: 'Bélgica', visitante: 'Irán', date: '2026-06-21', timeET: '15:00' },
  { id: 39, local: 'Uruguay', visitante: 'Cabo Verde', date: '2026-06-21', timeET: '18:00' },
  { id: 40, local: 'Nueva Zelanda', visitante: 'Egipto', date: '2026-06-21', timeET: '21:00' },
  { id: 41, local: 'Argentina', visitante: 'Austria', date: '2026-06-22', timeET: '13:00' },
  { id: 42, local: 'Francia', visitante: 'Irak', date: '2026-06-22', timeET: '17:00' },
  { id: 43, local: 'Noruega', visitante: 'Senegal', date: '2026-06-22', timeET: '20:00' },
  { id: 44, local: 'Jordania', visitante: 'Argelia', date: '2026-06-23', timeET: '00:00' },
  { id: 45, local: 'Portugal', visitante: 'Uzbekistán', date: '2026-06-23', timeET: '13:00' },
  { id: 46, local: 'Inglaterra', visitante: 'Ghana', date: '2026-06-23', timeET: '16:00' },
  { id: 47, local: 'Panamá', visitante: 'Croacia', date: '2026-06-23', timeET: '19:00' },
  { id: 48, local: 'Colombia', visitante: 'RD Congo', date: '2026-06-24', timeET: '00:00' },
  { id: 49, local: 'Bosnia y Herzegovina', visitante: 'Catar', date: '2026-06-24', timeET: '15:00' },
  { id: 50, local: 'Suiza', visitante: 'Canadá', date: '2026-06-24', timeET: '15:00' },
  { id: 51, local: 'Escocia', visitante: 'Brasil', date: '2026-06-24', timeET: '18:00' },
  { id: 52, local: 'Marruecos', visitante: 'Haití', date: '2026-06-24', timeET: '18:00' },
  { id: 53, local: 'Chequia', visitante: 'México', date: '2026-06-24', timeET: '21:00' },
  { id: 54, local: 'Sudáfrica', visitante: 'Corea del Sur', date: '2026-06-24', timeET: '21:00' },
  { id: 55, local: 'Ecuador', visitante: 'Alemania', date: '2026-06-25', timeET: '16:00' },
  { id: 56, local: 'Curazao', visitante: 'Costa de Marfil', date: '2026-06-25', timeET: '16:00' },
  { id: 57, local: 'Túnez', visitante: 'Países Bajos', date: '2026-06-25', timeET: '19:00' },
  { id: 58, local: 'Japón', visitante: 'Suecia', date: '2026-06-25', timeET: '19:00' },
  { id: 59, local: 'Turquía', visitante: 'Estados Unidos', date: '2026-06-25', timeET: '22:00' },
  { id: 60, local: 'Paraguay', visitante: 'Australia', date: '2026-06-25', timeET: '22:00' },
  { id: 61, local: 'Noruega', visitante: 'Francia', date: '2026-06-26', timeET: '15:00' },
  { id: 62, local: 'Senegal', visitante: 'Irak', date: '2026-06-26', timeET: '15:00' },
  { id: 63, local: 'Cabo Verde', visitante: 'Arabia Saudita', date: '2026-06-26', timeET: '20:00' },
  { id: 64, local: 'Uruguay', visitante: 'España', date: '2026-06-26', timeET: '20:00' },
  { id: 65, local: 'Egipto', visitante: 'Irán', date: '2026-06-26', timeET: '23:00' },
  { id: 66, local: 'Nueva Zelanda', visitante: 'Bélgica', date: '2026-06-26', timeET: '23:00' },
  { id: 67, local: 'Croacia', visitante: 'Ghana', date: '2026-06-27', timeET: '17:00' },
  { id: 68, local: 'Panamá', visitante: 'Inglaterra', date: '2026-06-27', timeET: '17:00' },
  { id: 69, local: 'Colombia', visitante: 'Portugal', date: '2026-06-27', timeET: '19:30' },
  { id: 70, local: 'RD Congo', visitante: 'Uzbekistán', date: '2026-06-27', timeET: '19:30' },
  { id: 71, local: 'Argelia', visitante: 'Austria', date: '2026-06-27', timeET: '22:00' },
  { id: 72, local: 'Jordania', visitante: 'Argentina', date: '2026-06-27', timeET: '22:00' },
];

// Diccionario bilingüe: token canónico -> variantes aceptadas (es/en y abreviaturas)
const TEAM_ALIASES = {
  mexico: ['mexico', 'mex'],
  sudafrica: ['sudafrica', 'south africa', 'rsa'],
  'corea del sur': ['corea del sur', 'corea republica', 'corea', 'south korea', 'korea republic', 'korea', 'kor'],
  chequia: ['chequia', 'republica checa', 'czechia', 'czech republic', 'cze'],
  canada: ['canada', 'can'],
  'bosnia y herzegovina': ['bosnia y herzegovina', 'bosnia herzegovina', 'bosnia', 'bosnia and herzegovina', 'bih'],
  catar: ['catar', 'qatar', 'qat'],
  suiza: ['suiza', 'switzerland', 'sui', 'swi'],
  brasil: ['brasil', 'brazil', 'bra'],
  marruecos: ['marruecos', 'morocco', 'mar'],
  haiti: ['haiti', 'hai'],
  escocia: ['escocia', 'scotland', 'sco'],
  paraguay: ['paraguay', 'par'],
  'estados unidos': ['estados unidos', 'eeuu', 'ee uu', 'usa', 'united states', 'estados unidos de america'],
  australia: ['australia', 'aus'],
  turquia: ['turquia', 'turkiye', 'turkey', 'tur'],
  alemania: ['alemania', 'germany', 'ger', 'ale'],
  curazao: ['curazao', 'curacao', 'cur'],
  'paises bajos': ['paises bajos', 'holanda', 'netherlands', 'ned', 'hol'],
  japon: ['japon', 'japan', 'jpn', 'jap'],
  'costa de marfil': ['costa de marfil', 'ivory coast', 'cote divoire', 'civ'],
  ecuador: ['ecuador', 'ecu'],
  suecia: ['suecia', 'sweden', 'swe'],
  tunez: ['tunez', 'tunisia', 'tun'],
  espana: ['espana', 'spain', 'esp'],
  'cabo verde': ['cabo verde', 'caboverde', 'islas de cabo verde', 'islas del cabo verde', 'islas cabo verde', 'cape verde', 'cape verde islands', 'cv islands', 'cpv'],
  belgica: ['belgica', 'belgium', 'bel'],
  egipto: ['egipto', 'egypt', 'egy'],
  iran: ['iran', 'irn'],
  'nueva zelanda': ['nueva zelanda', 'nueva zelandia', 'new zealand', 'nzl'],
  'arabia saudita': ['arabia saudita', 'arabia saudi', 'saudi arabia', 'ksa', 'arsa'],
  uruguay: ['uruguay', 'uru'],
  francia: ['francia', 'france', 'fra'],
  senegal: ['senegal', 'sen'],
  irak: ['irak', 'iraq', 'irq'],
  noruega: ['noruega', 'norway', 'nor'],
  argentina: ['argentina', 'arg'],
  argelia: ['argelia', 'algeria', 'alg'],
  austria: ['austria', 'aut'],
  jordania: ['jordania', 'jordan', 'jor'],
  portugal: ['portugal', 'por'],
  'rd congo': ['rd congo', 'rdc', 'republica democratica del congo', 'congo dr', 'dr congo', 'cod', 'congo'],
  uzbekistan: ['uzbekistan', 'uzb'],
  colombia: ['colombia', 'col'],
  inglaterra: ['inglaterra', 'england', 'eng'],
  croacia: ['croacia', 'croatia', 'cro'],
  ghana: ['ghana', 'gha'],
  panama: ['panama', 'pan'],
};

// Mapa invertido: variante normalizada -> token canónico
const ALIAS_LOOKUP = (() => {
  const map = {};
  for (const [canon, variants] of Object.entries(TEAM_ALIASES)) {
    map[canon] = canon;
    for (const v of variants) map[normalizeRaw(v)] = canon;
  }
  return map;
})();

function normalizeRaw(str) {
  return String(str || '')
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[\u{1F1E6}-\u{1F1FF}]/gu, '')
    .replace(/[^\w\s]/g, ' ')
    .toLowerCase()
    .trim()
    .replace(/\s+/g, ' ');
}

/** Normaliza un nombre de equipo a su token canónico (resuelve es/en y abreviaturas). */
export function normTeam(str) {
  const base = normalizeRaw(str);
  if (!base) return '';
  if (ALIAS_LOOKUP[base]) return ALIAS_LOOKUP[base];
  const words = base.split(' ');
  for (const w of words) {
    if (w.length > 2 && ALIAS_LOOKUP[w]) return ALIAS_LOOKUP[w];
  }
  return base;
}

export function teamsMatch(a, b) {
  const na = normTeam(a);
  const nb = normTeam(b);
  if (!na || !nb) return false;
  return na === nb;
}

/** Convierte fecha+hora ET (EDT) a Date absoluto */
export function etToDate(dateStr, timeET) {
  const [hh, mm] = timeET.split(':').map(Number);
  const iso = `${dateStr}T${String(hh).padStart(2, '0')}:${String(mm).padStart(2, '0')}:00-04:00`;
  return new Date(iso);
}

export function fixtureMatchesPartido(fixture, partido) {
  const homeOk = teamsMatch(fixture.local, partido.local) && teamsMatch(fixture.visitante, partido.visitante);
  const swapOk = teamsMatch(fixture.local, partido.visitante) && teamsMatch(fixture.visitante, partido.local);
  return homeOk || swapOk;
}

/** Busca en el calendario oficial el partido con los mismos dos equipos (sin importar el orden/id). */
export function findFixtureForPartido(partido) {
  return FIXTURES.find(fx => fixtureMatchesPartido(fx, partido)) || null;
}

export function formatDateCDMX(date) {
  return date.toLocaleString('es-MX', {
    weekday: 'short',
    day: '2-digit',
    month: 'short',
    hour: '2-digit',
    minute: '2-digit',
    timeZone: 'America/Mexico_City',
  });
}

/** Clave de día (YYYY-MM-DD) en hora del centro de México, para agrupar partidos. */
export function dayKeyCDMX(date) {
  return date.toLocaleDateString('en-CA', { timeZone: 'America/Mexico_City' });
}

/** Encabezado de día: "Sábado 13 de junio" (hora del centro de México). */
export function formatDayHeaderCDMX(date) {
  const s = date.toLocaleDateString('es-MX', {
    weekday: 'long',
    day: 'numeric',
    month: 'long',
    timeZone: 'America/Mexico_City',
  });
  return s.charAt(0).toUpperCase() + s.slice(1);
}

/** Etiqueta corta para tabs de día: "11 jun". */
export function formatDayTabCDMX(date) {
  return date.toLocaleDateString('es-MX', {
    day: 'numeric',
    month: 'short',
    timeZone: 'America/Mexico_City',
  }).replace('.', '');
}

// ============================================================
// Grupos — composición oficial fase de grupos (nombres en español)
// ============================================================
export const GROUPS = {
  A: ['México', 'Sudáfrica', 'Corea del Sur', 'Chequia'],
  B: ['Canadá', 'Bosnia y Herzegovina', 'Catar', 'Suiza'],
  C: ['Brasil', 'Marruecos', 'Haití', 'Escocia'],
  D: ['Estados Unidos', 'Paraguay', 'Australia', 'Turquía'],
  E: ['Alemania', 'Curazao', 'Costa de Marfil', 'Ecuador'],
  F: ['Países Bajos', 'Japón', 'Suecia', 'Túnez'],
  G: ['España', 'Cabo Verde', 'Arabia Saudita', 'Uruguay'],
  H: ['Bélgica', 'Egipto', 'Irán', 'Nueva Zelanda'],
  I: ['Francia', 'Senegal', 'Noruega', 'Irak'],
  J: ['Argentina', 'Argelia', 'Austria', 'Jordania'],
  K: ['Portugal', 'RD Congo', 'Colombia', 'Uzbekistán'],
  L: ['Inglaterra', 'Croacia', 'Ghana', 'Panamá'],
};

export const GROUP_LETTERS = Object.keys(GROUPS);

const TEAM_FLAGS = {
  mexico: '🇲🇽',
  sudafrica: '🇿🇦',
  'corea del sur': '🇰🇷',
  chequia: '🇨🇿',
  canada: '🇨🇦',
  'bosnia y herzegovina': '🇧🇦',
  catar: '🇶🇦',
  suiza: '🇨🇭',
  brasil: '🇧🇷',
  marruecos: '🇲🇦',
  haiti: '🇭🇹',
  escocia: '🏴󠁧󠁢󠁳󠁣󠁴󠁿',
  paraguay: '🇵🇾',
  'estados unidos': '🇺🇸',
  australia: '🇦🇺',
  turquia: '🇹🇷',
  alemania: '🇩🇪',
  curazao: '🇨🇼',
  'costa de marfil': '🇨🇮',
  ecuador: '🇪🇨',
  'paises bajos': '🇳🇱',
  japon: '🇯🇵',
  suecia: '🇸🇪',
  tunez: '🇹🇳',
  espana: '🇪🇸',
  'cabo verde': '🇨🇻',
  belgica: '🇧🇪',
  egipto: '🇪🇬',
  iran: '🇮🇷',
  'nueva zelanda': '🇳🇿',
  'arabia saudita': '🇸🇦',
  uruguay: '🇺🇾',
  francia: '🇫🇷',
  senegal: '🇸🇳',
  irak: '🇮🇶',
  noruega: '🇳🇴',
  argentina: '🇦🇷',
  argelia: '🇩🇿',
  austria: '🇦🇹',
  jordania: '🇯🇴',
  portugal: '🇵🇹',
  'rd congo': '🇨🇩',
  uzbekistan: '🇺🇿',
  colombia: '🇨🇴',
  inglaterra: '🏴󠁧󠁢󠁥󠁮󠁧󠁿',
  croacia: '🇭🇷',
  ghana: '🇬🇭',
  panama: '🇵🇦',
};

const TEAM_DISPLAY = (() => {
  const map = {};
  for (const teams of Object.values(GROUPS)) {
    for (const name of teams) map[normTeam(name)] = name;
  }
  return map;
})();

/** Nombre en español para mostrar (acepta nombre en es/en). */
export function displayTeamName(name) {
  const canon = normTeam(name);
  return TEAM_DISPLAY[canon] || name;
}

/** Bandera emoji del equipo. */
export function teamFlag(name) {
  return TEAM_FLAGS[normTeam(name)] || '⚽';
}

function emptyStandingRow(team) {
  return { team, j: 0, g: 0, e: 0, p: 0, gf: 0, gc: 0, dif: 0, pts: 0 };
}

function findGroupLetter(teamA, teamB) {
  const na = normTeam(teamA);
  const nb = normTeam(teamB);
  for (const [letter, teams] of Object.entries(GROUPS)) {
    const canon = teams.map(normTeam);
    if (canon.includes(na) && canon.includes(nb)) return letter;
  }
  return null;
}

/** Letra de grupo (A–L) para un partido, o null si no se reconoce. */
export function getMatchGroup(local, visitante) {
  return findGroupLetter(local, visitante);
}

function sortStandingsRows(rows) {
  return rows.slice().sort((a, b) => {
    if (b.pts !== a.pts) return b.pts - a.pts;
    if (b.dif !== a.dif) return b.dif - a.dif;
    if (b.gf !== a.gf) return b.gf - a.gf;
    return displayTeamName(a.team).localeCompare(displayTeamName(b.team), 'es');
  });
}

function applyMatchResult(rowsByCanon, local, visitante, gl, gv) {
  const home = rowsByCanon[normTeam(local)];
  const away = rowsByCanon[normTeam(visitante)];
  if (!home || !away) return;

  home.j += 1;
  away.j += 1;
  home.gf += gl;
  home.gc += gv;
  away.gf += gv;
  away.gc += gl;
  home.dif = home.gf - home.gc;
  away.dif = away.gf - away.gc;

  if (gl > gv) {
    home.g += 1;
    home.pts += 3;
    away.p += 1;
  } else if (gl < gv) {
    away.g += 1;
    away.pts += 3;
    home.p += 1;
  } else {
    home.e += 1;
    away.e += 1;
    home.pts += 1;
    away.pts += 1;
  }
}

/** Calcula tablas de grupo a partir de partidos con resultado (Firestore). */
export function computeGroupStandings(partidos) {
  const standings = {};
  for (const letter of GROUP_LETTERS) {
    standings[letter] = GROUPS[letter].map(emptyStandingRow);
  }

  for (const m of partidos) {
    if (m.golesLocal === null || m.golesVisitante === null) continue;
    const gl = Number(m.golesLocal);
    const gv = Number(m.golesVisitante);
    if (!Number.isFinite(gl) || !Number.isFinite(gv)) continue;

    const letter = findGroupLetter(m.local, m.visitante);
    if (!letter) continue;

    const rowsByCanon = {};
    for (const row of standings[letter]) {
      rowsByCanon[normTeam(row.team)] = row;
    }
    applyMatchResult(rowsByCanon, m.local, m.visitante, gl, gv);
  }

  for (const letter of GROUP_LETTERS) {
    standings[letter] = sortStandingsRows(standings[letter]);
  }
  return standings;
}

/** Convierte respuesta de wcup2026.org a tablas con nombres en español. */
export function parseApiStandings(apiStandings) {
  const standings = {};
  for (const letter of GROUP_LETTERS) {
    const key = `Group ${letter}`;
    const rows = apiStandings?.[key] || [];
    const byCanon = {};
    for (const row of rows) {
      byCanon[normTeam(row.team)] = {
        team: displayTeamName(row.team),
        j: row.p ?? 0,
        g: row.w ?? 0,
        e: row.d ?? 0,
        p: row.l ?? 0,
        gf: row.gf ?? 0,
        gc: row.ga ?? 0,
        dif: row.gd ?? 0,
        pts: row.pts ?? 0,
      };
    }
    standings[letter] = sortStandingsRows(
      GROUPS[letter].map(team => byCanon[normTeam(team)] || emptyStandingRow(team))
    );
  }
  return standings;
}

function totalPlayed(rows) {
  return rows.reduce((sum, r) => sum + r.j, 0);
}

/** Prefiere la fuente con más partidos jugados por grupo; empate → Firestore. */
export function mergeStandings(localStandings, apiStandings) {
  const merged = {};
  for (const letter of GROUP_LETTERS) {
    const local = localStandings[letter] || GROUPS[letter].map(emptyStandingRow);
    const remote = apiStandings?.[letter];
    if (!remote) {
      merged[letter] = local;
      continue;
    }
    merged[letter] = totalPlayed(remote) > totalPlayed(local) ? remote : local;
  }
  return merged;
}
